task_id: "20260620-001"
date: "2026-06-20"
status: "in_progress"

# === 任務定義 ===
goal: "把線性的四層 GATE_POLICY 收斂成有界、會終止、可稽核的任務內驗證閉環，並提供可執行驗證驅動器與測試"
context: |
  目前 system/GATE_POLICY.yaml 的四層驗證（schema→rule→completion→risk）是一次性線性管線，
  只有 schema_check 有可執行實作（system/validate_task_card.py），其餘三層靠 LLM 自我約束，
  且沒有「驗證→修正→再驗證」的迭代與可稽核帳本。本任務新增驗證閉環規格 + 可執行驅動器 +
  測試，讓閉環本身成為可被檢驗的工程。對齊計畫 /root/.claude/plans/loop-compiled-metcalfe.md。
input_data:
  - "system/GATE_POLICY.yaml"
  - "system/EXECUTION_LOG_SCHEMA.yaml"
  - "system/validate_task_card.py"
  - "scripts/permissions_guard.py"

# === 完成標準 ===
definition_of_done:
  - "新增 system/VERIFICATION_LOOP.yaml：定義閉環演算法、各 gate disposition、終態、迭代預算（≤3）與帳本 schema"
  - "GATE_POLICY.yaml header 加閉環交叉引用；EXECUTION_LOG_SCHEMA.yaml 新增向後相容的 verification_loop 區塊；CLAUDE.md 第 6 步指向 VERIFICATION_LOOP.yaml"
  - "新增 scripts/verification_loop.py：跑四層 gate、判 disposition、守迭代預算、產帳本，重用 validate_task_card.validate 與 PERMISSIONS deny 清單"
  - "scripts/test_verification_loop.py 與 tests/e2e/test_verification_loop_drill.py（pass / hard_stop / escalated / exhausted 四路徑）全綠"
  - ".github/workflows/spec-consistency.yml 新增執行上述測試的步驟；既有 CI 檢查（check_spec_consistency.rb、failure_drill、frontend manifest）仍全綠"

# === 輸出定義 ===
expected_output:
  format: "mixed"
  location: "outputs/drafts/"
  filename: "20260620-001_verification-loop-summary.md"

# === 風險與權限 ===
risk_level: "medium"
approval_needed: true
allowed_tools:
  - "file_read"
  - "file_search"
  - "write_drafts"
  - "create_output_files"
  - "write_logs"
  - "git_commit_checkpoint"
  - "bash"

# === 成本控制 ===
max_tool_calls: 40
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "ops"

# === 執行紀錄（由 agent 填寫）===
checkpoints: []
actual_tool_calls: 0
result_summary: ""
completion_time: ""
audit_log_ref: ""
