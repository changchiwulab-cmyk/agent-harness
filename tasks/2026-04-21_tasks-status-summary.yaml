# Task Card — tasks/ 目錄任務狀態彙整（Ops）

task_id: "20260421-O01"
date: "2026-04-21"
status: "done"

# === 任務定義 ===
goal: "掃描 tasks/ 目錄下所有 Task Cards，產出結構化的任務狀態摘要表"
context: "隨著任務累積，tasks/ 目錄已有多張 Task Card，需要建立一份統一的狀態總覽，方便追蹤哪些任務已完成、哪些待處理。此為定期 ops 維護任務。"
input_data:
  - "tasks/*.yaml"

# === 完成標準 ===
definition_of_done:
  - "涵蓋 tasks/ 目錄下所有 .yaml 檔案（TASK_CARD_TEMPLATE.yaml 和 DECISION_LOG_TEMPLATE.yaml 除外，它們是模板不是任務）"
  - "每張 Task Card 有以下欄位：task_id / date / status / skill_type / goal"
  - "以 Markdown 表格格式輸出，依日期倒序排列"
  - "在表格後加一段統計摘要：總任務數、各 status 分佈、各 skill_type 分佈"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "tasks-status-summary.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: false
allowed_tools:
  - "file_read"
  - "file_search"

# === 成本控制 ===
max_tool_calls: 8
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "ops"

# === 執行紀錄（由 agent 填寫）===
checkpoints:
  - "checkpoint: 20260421-O01 ops 完成"
actual_tool_calls: 2
result_summary: "掃描 tasks/ 下 14 張 Task Cards（排除 2 個模板），產出 Markdown 表格含 task_id/date/status/skill_type/goal，依日期倒序，附統計摘要（狀態分佈/skill 分佈/時間分佈）。DoD 4/4 通過。"
completion_time: "2026-04-21"
audit_log_ref: "logs/AUDIT_LOG.md"
