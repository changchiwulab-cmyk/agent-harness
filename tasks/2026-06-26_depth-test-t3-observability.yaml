# Task Card — 深度測試研究 T3：可觀測性／紀錄防漂移（＋成本/context 橫切）
# 來源：使用者 /goal「做 3 個不同面向的深度測試研究」之第 3 面向
# 脈絡：自我評估給可觀測 6（短板）。本次實跑全套唯讀 CI/觀測腳本取得真實健康度，
#       並稽核 Task Card→AUDIT_LOG→logs/runs 端到端可追溯性。成本/context 作橫切視角併入。
# 安全：全唯讀（--check / 報告模式），不改任何受測檔。

task_id: "20260626-003"
date: "2026-06-26"
status: "in_progress"

# === 任務定義 ===
goal: "實跑 harness 全套唯讀觀測/防漂移腳本取得真實健康度，稽核端到端可追溯性與成本/context 餘裕，產出研究草稿"
context: |
  可觀測性是自評短板（6）：工具層紮實但工作流/業務層缺、run log 稀疏、token 多為零值。
  本研究實跑 generate_frontend_manifest --check（drift）、check_spec_consistency、governance_metrics（M1–M4）、
  check_context_budget（context 餘裕＝成本橫切）、check_decision_revisit，記錄真實 exit code 與輸出；
  再交叉比對 tasks/(done|failed) ↔ AUDIT_LOG ↔ logs/runs 找可追溯性斷點。
  使用者多選的「成本/context」面向於此併入：token_estimate 零值盤點、COST_POLICY 校準缺口、context budget 餘裕。
input_data:
  - "scripts/generate_frontend_manifest.py"
  - "scripts/check_spec_consistency.rb"
  - "scripts/governance_metrics.py"
  - "scripts/check_context_budget.rb"
  - "logs/AUDIT_LOG.md"

# === 完成標準 ===
definition_of_done:
  - "唯讀腳本實跑表：generate_frontend_manifest --check / check_spec_consistency / governance_metrics / check_context_budget / check_decision_revisit 各記真實 exit code 與關鍵輸出"
  - "可追溯性稽核：tasks/(status=done|failed) ↔ AUDIT_LOG ↔ logs/runs 交叉比對，列出斷點（缺 run log、token 零值、覆蓋率）"
  - "成本/context 橫切：context budget 餘裕（vs 3000 token 上限）、token_estimate 零值盤點、COST_POLICY analysis 校準缺口"
  - "三層可觀測性（工具/工作流/業務）現況評級與缺口，對照自評可觀測 6"
  - "產出 outputs/drafts/2026-06-26_depth-test-t3-observability.md，遵循 結論→已知事實→合理推論→待驗證→高風險假設→來源；缺口只提案不就地修"
  - "全程唯讀、不修改 system/／skills/／scripts/／CLAUDE.md"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "2026-06-26_depth-test-t3-observability.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: false
allowed_tools:
  - "file_read"
  - "file_search"
  - "create_output_files"
  - "write_drafts"
  - "git_commit_checkpoint"

# === 成本控制 ===
max_tool_calls: 15
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "research"

# === 執行紀錄（由 agent 填寫）===
checkpoints: []
actual_tool_calls: 0
result_summary: ""
completion_time: ""
audit_log_ref: "logs/AUDIT_LOG.md"
