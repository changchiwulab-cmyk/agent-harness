# Task Card — L2：Context engineering 原則形式化
# 來源：20260620-001 評估 roadmap L2（C 軸 context engineering 5/10）

task_id: "20260620-003"
date: "2026-06-20"
status: "review"

# === 任務定義 ===
goal: "把 JIT context 載入 / compaction 存活 / memory-as-component 寫成明文原則段，並在 GLOBAL_RULES 加一行指標（不破 3K 預算）"
context: |
  C 軸 context engineering 僅 5/10：有路徑引用與『20 輪壓縮』慣例，但未形式化。
  詳細原則放 COST_POLICY.md（不受 3K 限制）；GLOBAL_RULES.md 只加一行指標，須過 check_context_budget.rb。
input_data:
  - "system/COST_POLICY.md"
  - "system/GLOBAL_RULES.md"
  - "CLAUDE.md"

# === 完成標準 ===
definition_of_done:
  - "COST_POLICY.md 新增『Context Engineering 原則』段（JIT 載入、compaction 存活、memory-as-component）"
  - "GLOBAL_RULES.md 加一行指標引用，且 CLAUDE.md+GLOBAL_RULES 合計仍 ≤3,000 tokens（check_context_budget.rb 綠）"
  - "全套 CI-equivalent 綠"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "system/"
  filename: "COST_POLICY.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: false
allowed_tools:
  - "file_read"
  - "edit_system_files"
  - "git_commit_checkpoint"

# === 成本控制 ===
max_tool_calls: 8
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "ops"

# === 執行紀錄（由 agent 填寫）===
checkpoints:
  - "checkpoint: 20260620-003 L2 context engineering principles"
actual_tool_calls: 0
result_summary: "COST_POLICY 新增 context engineering 原則段；GLOBAL_RULES 加一行指標，context budget 仍 ≤3K。"
completion_time: "2026-06-20"
audit_log_ref: "logs/AUDIT_LOG.md"
