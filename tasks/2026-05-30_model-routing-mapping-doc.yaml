task_id: "20260530-W01"
date: "2026-05-30"
status: "done"

# === 任務定義 ===
goal: "產出『3 分類 × 5 skill × phase』模型路由映射說明草稿"
context: >
  使用者的 3 個任務分類（快速讀取/搜尋、測試、統整/策略規劃）與現有 5 個 skill 並非 1:1。
  需一份說明文件，講清楚 tier ↔ skill ↔ phase 的對應，以及 sub-agent model 參數的實際用法，
  供日後建立 Task Card 與執行時查表。產出為草稿，待人工確認後晉升 reports/。
input_data:
  - "system/MODEL_ROUTING.yaml"
  - "system/ROUTING_RULES.md"

# === 完成標準 ===
definition_of_done:
  - "outputs/drafts/ 產出映射說明，含 tier→model→reasoning_effort、3 分類→skill→phase 對應表、同卡多階段（phase_overrides）用法"
  - "說明 sub-agent model 參數如何落實路由（Explore→fast-reader 等）"
  - "明確標示限制：reasoning_effort 為宣告意圖、非程式強制"
  - "文件結構符合 writing skill 規範，可讀可查"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "2026-05-30_model-routing-mapping.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: false
allowed_tools:
  - "file_read"
  - "write_drafts"
  - "git_commit_checkpoint"

# === 成本控制 ===
max_tool_calls: 10
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "writing"

# === 執行紀錄 ===
checkpoints: []
actual_tool_calls: 0
result_summary: "產出 outputs/drafts/2026-05-30_model-routing-mapping.md：tier→模型→reasoning_effort→子代理對照、3 分類×skill×phase 映射、phase_overrides 同卡多階段用法、resolution_order、限制揭露（reasoning_effort 非強制）。草稿待人工晉升 reports/。"
completion_time: "2026-05-30"
audit_log_ref: "logs/AUDIT_LOG.md"
