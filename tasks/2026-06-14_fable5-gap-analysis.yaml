task_id: "20260614-001"
date: "2026-06-14"
status: "in_progress"

# === 任務定義 ===
goal: "以 Fable 5 + Claude Code 原生能力為基準，盤點本框架的弱點並提出強化路線"
context: >
  使用者要求以前沿模型 + 原生能力為基準，找出 harness 的落差並做強化。
  優先序：先完整 gap 盤點 → 能力擴充 → 強制力與可觀測（不主打砍冗餘）。
  本卡負責 Phase 0：產出 gap 分析草稿並重新定錨 NATIVE_OVERLAP.yaml。
input_data:
  - "system/NATIVE_OVERLAP.yaml"
  - "system/ROUTING_RULES.md"
  - "system/COST_POLICY.md"
  - "scripts/permissions_guard.py"
  - "scripts/governance_metrics.py"

# === 完成標準 ===
definition_of_done:
  - "產出 gap 分析草稿，涵蓋能力/強制力/可觀測三類落差，每項附證據與嚴重度"
  - "提出分階段強化路線（強制力 → 可觀測 → 能力）並標明需人工核可的項目"
  - "重新定錨 system/NATIVE_OVERLAP.yaml（更新評估日期與逐模組數值）"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "2026-06-14_fable5-baseline-gap-analysis.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: true
allowed_tools:
  - "read_project_files"
  - "file_search"
  - "write_drafts"
  - "git_commit_checkpoint"

# === 成本控制 ===
max_tool_calls: 8
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "analysis"

# === 執行紀錄（由 agent 填寫）===
checkpoints: []
actual_tool_calls: 0
result_summary: ""
completion_time: ""
audit_log_ref: ""
