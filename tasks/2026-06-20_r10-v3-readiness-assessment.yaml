# Task Card — R10 v3 遷移就緒度評估（只評估不遷移）
# 來源：workflow 優化計畫「收尾：roadmap R10」

task_id: "20260620-006"
date: "2026-06-20"
status: "review"

# === 任務定義 ===
goal: "產出 v3 遷移就緒度評估草稿：逐模組標保留/下放原生/並存 + 就緒度判定，盤點不可替代資產（含本批 runtime 強制層），對齊 D003/D007 並給 Go/No-Go 建議"
context: |
  A01（2026-05-09_v3_extraction_plan.md）已做『怎麼抽』；R10 做『現在準備好了嗎』。
  本批 R11–R14 的 runtime 強制層直接消解 A01 §1.3 症狀#1（強制力頭重腳輕），
  使 Gate Verifier 就緒度由『待抽出』升為 ready，並成為 v3 plugin hooks 的可攜資產。
  結論需誠實：技術就緒提升、但 D003 的 v3 需求觸發條件（context 超限/規則衝突）未達標 → Conditional Hold。
  只評估、不動結構；草稿先進 drafts，待人工依 RETRO_FLOW 晉升。
input_data:
  - "outputs/drafts/2026-05-09_v3_extraction_plan.md"
  - "system/NATIVE_OVERLAP.yaml"
  - "memory/active_projects/agent-harness/decisions/20260424-D006_execution-log-scope.yaml"
  - "scripts/gate_runner.py"

# === 完成標準 ===
definition_of_done:
  - "outputs/drafts/v3-readiness-assessment.md：16 模組逐項 保留/下放原生/並存 + 就緒度(ready/partial/blocked) + blocker"
  - "就緒度三軸評分（技術/抽出/需求）對比 A01 當時 vs 本評估"
  - "不可替代資產清單新增第五類 runtime 強制層（gate_runner/failure_tracker/write_guard）"
  - "對齊 D003（建議維持 hold 但更新理由 + 細化 revisit_trigger）與 D007（plugin hooks 移植本批強制層）"
  - "Go/No-Go 建議表 + 待驗證假設（含新增 H4）"

# === 輸出定義 ===
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "v3-readiness-assessment.md"

# === 風險與權限 ===
risk_level: "low"
approval_needed: false
allowed_tools:
  - "file_read"
  - "write_drafts"
  - "create_output_files"
  - "git_commit_checkpoint"

# === 成本控制 ===
max_tool_calls: 12
max_retries: 3
max_web_searches: 0

# === Skill 路由 ===
skill_type: "analysis"

# === 執行紀錄（由 agent 填寫）===
checkpoints: []
actual_tool_calls: 0
result_summary: "產出 v3 就緒度評估草稿：ready 10/partial 6/blocked 0；Gate Verifier 因本批 gate_runner 由待抽出升 ready；新增 runtime 強制層為第五類不可替代資產。結論 Conditional Hold（技術就緒提升、需求觸發未達標），建議維持 D003 但更新理由、hold 期間先把強制層整理為 plugin hooks 雛形。"
completion_time: ""
audit_log_ref: "logs/AUDIT_LOG.md"
