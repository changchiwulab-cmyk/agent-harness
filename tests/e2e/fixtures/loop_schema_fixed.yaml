# verification-loop drill fixture (schema-fixable path) — iteration 2 (corrected).
# The agent's "fix" applied between loop iterations: definition_of_done filled,
# skill_type valid. With --completion all-pass + drafts/ location + clean tools,
# all four gates pass → loop outcome = pass. Lives outside tasks/ (see sibling).

task_id: "20260620-901"
date: "2026-06-20"
status: "in_progress"
goal: "示範 schema 可修路徑：第 2 輪修正後通過"
definition_of_done:
  - "補上 definition_of_done"
  - "skill_type 改為合法值"
skill_type: "ops"
risk_level: "low"
expected_output:
  format: "md"
  location: "outputs/drafts/"
  filename: "loop_drill.md"
allowed_tools:
  - "file_read"
  - "write_drafts"
max_retries: 3
