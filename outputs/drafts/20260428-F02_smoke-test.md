# 20260428-F02 — Phase 1 本地 smoke test 紀錄

執行時間：2026-04-28
執行者：agent (claude-opus-4-7)
分支：`claude/go-setup-zu6Dw`

## 1. Generator + 漂移檢查

```
$ python3 scripts/generate_frontend_manifest.py
Generated frontend/data.json (17 tasks, 1 logs, 6 decisions)

$ python3 scripts/generate_frontend_manifest.py --check
OK: frontend/data.json is up to date.
```

## 2. 單元測試（test_generate_frontend_manifest.py，9 case）

```
$ python3 scripts/test_generate_frontend_manifest.py -v
test_check_mode_detects_missing_output ............................. ok
test_decision_extended_fields_collected ............................ ok
test_empty_repo_yields_empty_collections ........................... ok
test_failure_taxonomy_real_file_complete ........................... ok
test_idempotent_output_is_byte_identical ........................... ok
test_log_approvals_array_preserved ................................. ok
test_log_gate_results_round_trip ................................... ok
test_multi_project_decisions_are_all_collected ..................... ok
test_system_meta_loads_three_files ................................. ok

Ran 9 tests in 0.028s — OK
```

## 3. CI 等價檢查（本地預跑）

```
$ scripts/check_spec_consistency.rb
OK: spec consistency checks passed

$ ruby scripts/test_check_spec_consistency.rb
14 runs, 43 assertions, 0 failures, 0 errors, 0 skips

$ ruby scripts/check_context_budget.rb
TOTAL ~1197 tokens (budget 3000)
OK: context budget within limit
```

## 4. 端對端 smoke：run_frontend.sh + curl

```
$ scripts/run_frontend.sh --no-generate 18742 &
Frontend available at: http://localhost:18742/frontend/index.html
Serving HTTP on 0.0.0.0 port 18742 ...

$ curl -sf http://localhost:18742/frontend/data.json -o /tmp/smoke.json
HTTP OK, size=26734 bytes
```

`data.json` 結構（核心欄位）：

| 區塊 | 鍵 |
|---|---|
| top-level | `decisions`, `logs`, `system_meta`, `tasks` |
| `logs[0]` | `approvals`, `checkpoints`, `ended_at`, `error_summary`, `gate_results`, `notes`, `path`, `run_id`, `skill_type`, `started_at`, `status`, `task_id`, `token_estimate`, `tools_used` |
| `system_meta` | `approval_policy`, `failure_taxonomy`, `gate_policy` |
| `failure_taxonomy.categories` | `coordination`, `security`, `spec`, `validation` |
| `decisions[0]` | `date`, `decision`, `decision_id`, `options_considered`, `path`, `project`, `reasoning`, `related_task`, `revisit_trigger`, `risk`, `status` |

HTML 已被服務的關鍵元素：`#gateMatrix`、`#approvalTrail`、`#failureMap`、`#checkpointChain`、`#filterFailures`。

## 5. 肉眼瀏覽器檢查

說明：本 session 為無頭 sandbox，無法開啟真實瀏覽器渲染。
- 已透過 `curl` 確認 `data.json` 與 `index.html` 皆能由 `python3 -m http.server` 正常提供。
- HTML 中四個 panel 容器與「只看失敗 / Gate Fail」按鈕 id 全部存在。
- app.js 透過 ES module + 純 DOM API 渲染，對舊版瀏覽器以外的環境無相依需求。

建議使用者於本機跑 `scripts/run_frontend.sh` 後肉眼確認：
1. 載入即顯示 4 個 panel summary（Gate Matrix 預設展開，其餘預設收合）
2. 點開 Gate Matrix 中某 cell，能展開該層 GATE_POLICY 描述
3. Failure Map 14+1 格皆可懸停顯示 mitigation tooltip
4. Approval Trail 出現 1 筆 RUN-20260409-001 的兩個 approval 事件
5. Checkpoint Chain 對 RUN-20260409-001 顯示三段（commit 為 "pending" 文字而非 hex，會以 italic 呈現）
6. 點「只看失敗 / Gate Fail」：目前資料無 fail，列表會被清空（按鈕本身亮起黑底）

## 6. 結論

DoD #12（本地 smoke test）：自動化部分（generator、tests、CI 等價、HTTP 服務、JSON shape）全部 PASS。
肉眼瀏覽器確認需使用者於有 GUI 的環境執行 `scripts/run_frontend.sh` 後 attach 截圖到本檔。
